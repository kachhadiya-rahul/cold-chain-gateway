# Cold-Chain Gateway

ASP.NET Core service for refrigerated-container telemetry.

```bash
dotnet run --project src/ColdChain.Gateway
```

Dispatcher (needs the gateway running):

```bash
cd web/dispatcher && npm start
```

